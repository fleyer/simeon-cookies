pre-commit:
  parallel: true
  commands:
    typecheck:
      run: bun run typecheck
    lint:
      glob: "*.{js,ts,vue}"
      run: bunx oxlint {staged_files} && bunx eslint --fix {staged_files}
      stage_fixed: true
